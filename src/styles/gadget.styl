@import 'nib'

global-reset()

// textarea
.code
  width 100%

.code, .hljs > code, .hljs > code > *, .hljs 
  font-family monospace
  font-size 0.95em
  line-height 1.1em

textarea
  line-height 15px
  line-height 1.2em
